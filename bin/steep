#!/bin/sh

GEMFILE=$(cd $(dirname $0); pwd)/../steep/Gemfile

if type rbenv > /dev/null 2>&1; then
  rbenv exec bundle exec --gemfile=${GEMFILE} steep $*
else
  bundle exec --gemfile=${GEMFILE} steep $*
fi
